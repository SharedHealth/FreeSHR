CREATE TABLE FACILITIES(
    facility_id varchar primary key,
    facility_name varchar,
    facility_type varchar,
    catchments varchar,
    division_id varchar,
    district_id varchar,
    upazilla_id varchar,
    city_corporation_id varchar,
    ward_id varchar
);